import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jorgemorales.taskmanager',
  appName: 'ionic-smart-tasks',
  webDir: 'www/browser',
};

export default config;
